const valores = [7.7, 7.8, 8.2, 4.5];
console.log(valores[0], valores[3]);
console.log(valores.length); // mostra o tamanho do vetor

console.log(valores.pop()); // ele excluí o último elemento do vetor
console.log(valores);
delete valores[0]; 
console.log(valores);

console.log(typeof valores); // mostra o tipo do elemento
