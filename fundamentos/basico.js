valor = 5.7998;
console.log((valor).toFixed(2));

// Usando a biblioteca Math
const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2);
// Math.PI - mostra o número PI
// Math.pow - calculo exponencial
console.log((area).toFixed(2));
